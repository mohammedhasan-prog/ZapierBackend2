{
  "name": "zapier-monorepo",
  "private": true,
  "workspaces": [
    "primary_backend",
    "hooks",
    "processer",
    "worker",
    "frontend"
  ],
  "scripts": {
    "build": "npm install && npm run build --workspaces",
    "start": "echo '❌ ERROR: You are trying to deploy the ROOT directory. Go to Railway -> Settings -> Root Directory and set it to your service folder (e.g., primary_backend, hooks, etc.)' && exit 1"
  }
}
